<%= form_for(@cadavre) do |f| %>
  <% if @cadavre.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cadavre.errors.count, "error") %> prohibited this cadavre from being saved:</h2>

      <ul>
      <% @cadavre.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
